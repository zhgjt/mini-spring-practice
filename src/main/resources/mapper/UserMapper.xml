<?xml version="1.0" encoding="UTF-8"?>
<mapper namespace="org.example.entity.User">
    <select id="getUserInfo" parameterType="java.lang.Integer" resultType="org.example.entity.User">
        select id, name,birthday
        from users
        where id=?
    </select>
</mapper>